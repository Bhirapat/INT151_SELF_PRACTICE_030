การ export/import
ใช้ export {} เพื่อส่งออกหลาย ๆ อย่างจากไฟล์ เช่น ฟังก์ชัน ค่าคงที่ และ class

sort()

ใช้ localeCompare สำหรับเรียง string ตามพจนานุกรม

ใช้การเปรียบเทียบเชิงตัวเลข เช่น (a, b) => b - a สำหรับเรียงจากมากไปน้อย

filter() + map()

filter → เลือกเฉพาะข้อมูลที่ตรงเงื่อนไข (score ≥ 70)

map → แปลงค่าที่ได้ เช่น เปลี่ยนชื่อให้เป็นตัวพิมพ์ใหญ่